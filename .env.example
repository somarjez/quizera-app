# Flask Configuration
SECRET_KEY=your-secret-key-here
FLASK_ENV=production

# Firebase Configuration (JSON key content)
# Copy the entire content of your firebase-key.json file
# Or set GOOGLE_APPLICATION_CREDENTIALS path
FIREBASE_CREDENTIALS={"type": "service_account", "project_id": "your-project"}

# Email Configuration (if using Flask-Mail)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Additional Configuration
# Add any other environment variables from your config.py here
